# Lena Student Web - Environment Variables Example
# Copy this file to .env and fill in your actual values

# =============================================================================
# REQUIRED: Core Configuration
# =============================================================================

# Node Environment (affects security settings like cookie secure flag)
NODE_ENV=development

# Backend API Base URL (REQUIRED - used by our API client)
# Our app currently uses this via import.meta.env.VITE_LENA_API_URL
# Default: https://test.lena.africa (if not set)
VITE_LENA_API_URL=https://test.lena.africa

# =============================================================================
# Session Security (OPTIONAL but recommended for production)
# =============================================================================

# Session cookie signing secret (for signed/encrypted cookies)
# ⚠️  WARNING: VITE_ variables are exposed to the client browser!
# This is okay for cookie signing since React Router handles this securely,
# but NEVER put sensitive API keys or database credentials in VITE_ variables!
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# VITE_SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Session cookie settings (optional - these have sensible defaults)
# VITE_SESSION_MAX_AGE=604800          # 7 days in seconds
# VITE_SESSION_COOKIE_NAME=lena_token
# VITE_TEMP_EMAIL_COOKIE_NAME=temp-email
# VITE_TEMP_EMAIL_MAX_AGE=300          # 5 minutes in seconds

# =============================================================================
# DEVELOPMENT: Optional Overrides
# =============================================================================

# Development server port (React Router dev server)
# PORT=5173

# API request timeout in milliseconds (default: 10000ms)
# VITE_API_TIMEOUT=10000

# Enable debug logging (not yet implemented)
# DEBUG=true

# =============================================================================
# FUTURE: External Services
# =============================================================================

# These are placeholders for when we add these features:

# Email service (for password reset, notifications, etc.)
# EMAIL_SERVICE_API_KEY=your-email-service-key
# FROM_EMAIL=noreply@lena.africa

# File upload service (for profile pictures, documents)
# UPLOAD_SERVICE_URL=https://upload.lena.africa
# UPLOAD_API_KEY=your-upload-service-key

# Analytics/Error tracking
# ANALYTICS_ID=your-analytics-id
# SENTRY_DSN=your-sentry-dsn

# =============================================================================
# CURRENT STATE vs FUTURE IMPROVEMENTS
# =============================================================================

# ✅ What we use NOW:
# - NODE_ENV (for cookie security settings)
# - VITE_LENA_API_URL (for API base URL)

# 🔄 What we COULD implement:
# - SESSION_SECRET (for signed cookies)
# - Configurable cookie names and timeouts
# - API timeout configuration
# - Debug logging
# - Error tracking integration

# =============================================================================
# Environment-Specific Files
# =============================================================================

# You can create these files for different environments:
# - .env.local      (local development overrides, gitignored)
# - .env.production (production-only variables)
# - .env.test       (test environment variables)

# =============================================================================
# Vite Environment Variables Explained
# =============================================================================

# Vite has two types of environment variables:

# 1. SERVER-SIDE ONLY (process.env.*)
#    - Available only in server-side code (loaders, actions)
#    - Never exposed to the browser
#    - Use for sensitive data like API keys, database URLs
#    Examples: DATABASE_URL, STRIPE_SECRET_KEY

# 2. CLIENT-SIDE (import.meta.env.VITE_*)
#    - Available in both server and client code
#    - EXPOSED to the browser (visible in DevTools!)
#    - Must be prefixed with VITE_
#    - Use for non-sensitive configuration
#    Examples: VITE_API_URL, VITE_TIMEOUT

# Special Vite variables (always available):
# - import.meta.env.MODE ("development" or "production")
# - import.meta.env.PROD (true in production)
# - import.meta.env.DEV (true in development)

# =============================================================================
# How to Generate SESSION_SECRET
# =============================================================================

# Run this command to generate a secure session secret:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Or use online tools (but CLI is more secure):
# openssl rand -hex 32
